import {
  EMAIL_MISSING,
  PASSWORD_MISSING,
  INVALID_EMAIL,
  INVALID_PASSWORD,
} from "./errors";

const constants = {
  EMAIL_MISSING,
  PASSWORD_MISSING,
  INVALID_EMAIL,
  INVALID_PASSWORD,
};

export default constants;
